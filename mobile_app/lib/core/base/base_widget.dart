// lib/core/base/base_widget.dart

import 'package:flutter/material.dart';

/// Base cho các Widget custom dùng nhiều nơi
abstract class BaseWidget extends StatelessWidget {
  const BaseWidget({super.key});
}
