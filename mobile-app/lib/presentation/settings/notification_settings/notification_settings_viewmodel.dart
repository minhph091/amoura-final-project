import 'package:flutter/material.dart';

class NotificationSettingsViewModel extends ChangeNotifier {
  // Map<String, bool> for notification settings state
  // Getters and setters for notification states
}